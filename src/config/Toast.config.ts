const ToastConfig = {
  speed:{
    fast: 200,
    normal: 400,
    slow: 600,
  },
  default:{
    duration: 3000,
    position: 'top',
    margin: 75,
    speed: 'normal'
  }
}

export default ToastConfig;
